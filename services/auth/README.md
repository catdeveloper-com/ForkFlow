# Сервис аутентификации ForkFlow

## Текущий статус

Сервис можно запустить локально; он предоставляет только операционный маршрут
`GET /health`. Этот маршрут не содержит бизнес-логики и не подключается к
PostgreSQL или Redis.

В доменном слое уже описаны базовые строительные блоки регистрации:
email-адрес, сырой пароль, хеш пароля и пользователь как доменная сущность.
HTTP API регистрации пока не реализован.

В слое сценариев приложения добавлен use case регистрации пользователя. Он
создаёт доменные email и пароль, проверяет уникальность через порт хранилища,
хеширует пароль через порт хеширования и создаёт пользователя. Для
хеширования паролей добавлен инфраструктурный Argon2-адаптер. Реальный адаптер
репозитория PostgreSQL пока не добавлен: в тестах используется
fake-репозиторий.

В инфраструктурном слое добавлены SQLAlchemy-модель пользователя, async
session factory и сервисные Alembic-миграции внутри `services/auth`.

```bash
make run-auth
curl http://127.0.0.1:8000/health
```

Успешный ответ:

```json
{
  "status": "ok",
  "service": "auth"
}
```

## Конфигурация

Сервис читает настройки через `pydantic-settings`. Переменные окружения имеют
префикс `AUTH_`, а локальные значения можно положить в `.env`.

Сейчас используются только настройки метаданных FastAPI:

```env
AUTH_SERVICE_TITLE=ForkFlow Auth Service
AUTH_SERVICE_VERSION=0.1.0
AUTH_DATABASE_URL=postgresql+asyncpg://forkflow:local-development-only-change-me@localhost:5432/forkflow_auth
```

Если переменные не заданы, сервис использует такие же значения по умолчанию.
Настройки Redis, JWT, параметров password hasher и секретов пока не добавлены,
потому что сервис ещё не использует эти зависимости.

## Миграции

Миграции Auth-сервиса живут в `services/auth/alembic`.

```bash
make migrate-auth
make revision-auth MESSAGE="описание миграции"
```

Текущая миграция создаёт таблицу `users` с email, хешем пароля и timestamps.
SQLAlchemy repository для работы с этой таблицей будет добавлен отдельным PR.

## Планируемая ответственность

Сервис аутентификации будет отвечать за идентификацию и аутентификацию:
регистрацию пользователей, проверку учётных данных, жизненный цикл токенов и
сессий, а также роли авторизации. Другие сервисы не должны напрямую обращаться
к его базе данных.

## Внутренние границы

Сервис следует направлению зависимостей Clean Architecture, принятому в
репозитории:

```text
presentation -> application -> domain
infrastructure -> application/domain through ports
```

- `domain` содержит независимые от фреймворков сущности, value objects и
  доменные ошибки.
- `application` содержит use cases, DTO и порты.
- `infrastructure` содержит Argon2-адаптер хеширования паролей, SQLAlchemy
  модель пользователя, DB session factory и Alembic-миграции.
- `presentation` будет содержать HTTP-схемы и тонкие обработчики маршрутов.

Сейчас в коде есть `presentation` для healthcheck, `domain` для базовой модели
пользователя, `application` для сценария регистрации и `infrastructure` для
Argon2-хеширования паролей и PostgreSQL schema. Repository adapter будет
добавлен отдельно.

## Отложенная работа

HTTP API регистрации, сценарий входа, SQLAlchemy repository, токены и сессии
должны появляться отдельными сфокусированными изменениями.
